- clean: ["~"]

- shell:
    - command: ./shell.sh
      stdout: true
    - [git submodule update --init --recursive, Installing submodules]

- link:
    ~/.config/:
      glob: true
      path: config/*
      create: true
    ~/:
      glob: true
      path: root/*
      prefix: "."
