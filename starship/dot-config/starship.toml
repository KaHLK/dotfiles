add_newline = false
command_timeout = 200
format = "$directory$git_branch$git_state$git_status$character"
right_format = "$git_metrics$cmd_duration"

[character]
error_symbol = "[✗](bold red)"
success_symbol = "[❯](bold white)"

[directory]
truncation_length = 2
truncation_symbol = "…/"
style = "white"
repo_root_style = "bold green"
repo_root_format = "[$repo_root]($repo_root_style)[$path]($style)[$read_only]($read_only_style) "

[git_branch]
format = "[$symbol](green)[\\[$branch\\]](blue) "
symbol = " "
truncation_length = 16
only_attached = true

[git_status]
format     = """
[$ahead_behind]($style)\
[$conflicted](bg:red fg:black)\
[$modified](fg:red)\
[$staged](fg:yellow)\
[$untracked](fg:purple)\
"""
style      = "cyan"
ahead      = "⇡${count} "
diverged   = "⇕⇡${ahead_count}⇣${behind_count} "
behind     = "⇣${count} "
conflicted = " "
up_to_date = " "
untracked  = "?${count} "
modified   = "${count} "
stashed    = ""
staged     = "+${count} "
renamed    = ""
deleted    = ""

[git_metrics]
disabled = false
